import { StyleSheet, Text, TextInput, View } from "react-native";
import React from "react";
import { FontAwesome } from "@expo/vector-icons";

const FavoriteList = ({}) => {
    return (
        <View>
            <Text>AWiakwaoko</Text>
        </View>
    );
};

export default SearchFilter;

const styles = StyleSheet.create({});